# 数据闭环与预留能力说明

> 最后更新：2026-07-02  
> 与产品拍板一致：已实现项写清代码位置；预留项仅保留 schema/文档；未立项能力从主文档移除。

---

## 一、已实现闭环

### 1. 打卡效果/备注 → 次日 AI 方案（Q1 ✅）

用户在前端打卡填写的 `result`（训练效果）、`note`（备注）会进入昨日训练摘要，供豆包生成今日报告与排课参考。

| 环节 | 说明 |
|------|------|
| 写入 | `POST /api/training/checkin` → `training_record.result/note` 及 `files_json[].result/note` |
| 读取 | `training_service.get_yesterday_training_context()` |
| 消费 | `training_plan_generator.generate_daily_report_text()`、`training_route_context.build_route_context()` |

### 2. 答疑易错点/教练提示 → 后续对话（Q2 ✅）

| 环节 | 说明 |
|------|------|
| 写入 | `qa_service` 每次回复将 `coach_hint`、`mistake_pattern` 写入 `qa_message.meta_json` |
| 读取 | `qa_coach.fetch_recent_coach_context_for_prompt()` |
| 消费 | `build_qa_system_prompt(..., coach_context=...)` 注入系统提示 |

### 3. 昨日训练承接 → 今日续训（carryover ✅，🔄 v2.0）

| 环节 | 说明 |
|------|------|
| 写入 | 打卡完成时 `training_service` 更新各技能 `consecutive_pass` + OSS stage/part |
| 读取 | `training_carryover.build_carryover_from_yesterday()` |
| 消费 | `training_schedule_service` 排课时按技能续推（v2.0：不再按主线，按各技能独立状态） |

### 4. 训练日结算 → 凌晨4点截止（day settlement ✅）

| 环节 | 说明 |
|------|------|
| 写入 | 每日 04:00 日切，已完成方案锁定，未完成方案结算 |
| 读取 | `training_day_settlement.settle_training_day()` |
| 消费 | `training_service` 进页时检查日切状态，过期方案标记 stale |

### 5. 老学员引导 → 天赋落库（onboarding ✅）

| 环节 | 说明 |
|------|------|
| 写入 | `PUT /api/user/profile` → `profile_json.onboarding` → `sync_child_user_talent()` |
| 读取 | `onboarding_service` + `get_self_reported_talent_code()` |
| 消费 | 首页今日训练读取 `child_user.talent_code` 进行排课 |

---

## 二、预留（有字段/能力，暂不开发）

| 能力 | 位置 | 说明 |
|------|------|------|
| 打卡复核 | `training_record.review_status` 等 | 默认 `approved`，无审核 API；B 端上线时再补状态机 |
| 首页引导个性化 | `guide_service` / `guide_session` | 当前静态人设；后续可读 `profile_json`、训练进度 |
| 成长达成日精确回溯 | `growth_service._build_earned_at_map` | 当前部分徽章用查看日；后续按打卡记录回溯 |
| 答疑语音消息 | `qa_message.voice_url` | 当前仅文本+图片；语音答疑立项后再用 |
| 清北班标记 | `child_user.is_qingbei` | 仅 profile 返回，无专属训练逻辑 |

---

## 三、当前不做（已从主规格移除）

以下能力**不在当前版本范围**，文档与排期中不再作为待办：

- 训练记录运营审核后台（在未做 Q3 实现前不单独规划）
- `media_exhausted` 后台定时主动结算（仍由进页 `GET /today` 被动更新）
- 技能 Tier 晋级用户侧预告/推送（✅ v2.0：打卡时实时判定并返回晋级状态，不再 pending）
- 测评 `attributeList` 全量维度雷达图 API（前端报告页已用 JNAO 报告）
- `TalentAssessmentArchive` 查询/恢复接口
- `content_item.source_id/course_id` 对账自动化
- `jnao_uid` 与 JNAO 平台反查校验
- `time_spent` 历史趋势分析
- 视频/音频偏好自动排课

---

## 四、相关代码索引

```
# Q1 打卡→次日方案
backend/app/services/training_service.py          # get_yesterday_training_context
backend/app/services/training_child_guide.py      # LLM 教练文案生成
# Q2 答疑→后续对话
backend/app/services/qa_coach.py                  # fetch_recent_coach_context_for_prompt
backend/app/agents/qa/prompt_builder.py           # coach_context 参数
backend/app/services/qa_service.py
# Q3 昨日→今日续训（🔄 v2.0 按技能）
backend/app/services/training_carryover.py         # build_carryover_from_yesterday
backend/app/services/training_schedule_service.py  # 排课时检查 carryover
# Q4 日结算
backend/app/services/training_day_settlement.py    # settle_training_day
backend/app/services/training_day.py               # get_training_day / is_new_day_ready
# Q5 v2.0 晋级判定
backend/app/services/training_mastery.py           # 连续3次达标→Tier+1→OSS推进
backend/app/services/child_training_state.py       # 多技能状态机
backend/app/services/training_formula_engine.py    # 时长→技能组合展开
# 老学员引导
backend/app/services/onboarding_service.py         # 引导数据落库
backend/app/services/assessment_service.py         # sync_child_user_talent
```
