# 待办 2026-08-27

## P0 百炼 RAG 生产闭环（Retrieve → 豆包）

- [ ] 服务器 `.env.production` 补全并确认：
  - `BAILIAN_RAG_GENERATE=0`（必须，勿用 file_search 直答）
  - `GUIDE_RAG_ENABLED=1`、`GUIDE_RAG_MODE=retrieve`
  - `BAILIAN_RAG_FALLBACK_DOUBAO=1`
  - `BAILIAN_INDEX_ID=x1micrdmjq`
  - （可选训练页）`BAILIAN_VIDEO_INDEX_ID=l37mx47k4u`、`TRAINING_RAG_ENABLED=1`
- [ ] 拉 `backend/dev` 最新（含 `6fe4b67`）并 `docker compose up -d --build`
- [ ] 容器内冒烟：`guide_rag_query('超脑阅读怎么练')` → `nodes > 0`
- [ ] 首页问「超脑阅读怎么练」，日志应有 `guide rag mode=retrieve` + `rag_source=retrieve_doubao`
- [ ] 统一 `JNAO_BUILD_ID`（去掉重复项，发版改一次）

## P0 引导页 KB 回答质量（策略套话覆盖知识库）

现象：问「超脑阅读/开口窍怎么练」→ 回复全是「赢者小目标 + 去今日训练」，不像 KB 内容。

根因：Retrieve 可能命中 Tier/达标文档而非练习方法；同时 **赢者策略 + ready_to_train 情境 + 2–4 句先答后导** 优先级高于 `rag_block`。

- [ ] **有 KB 时强化 prompt**（`build_chat_system_prompt` / `runner.py`）  
  用户问「怎么练/如何练习」时：必须先从知识库参考提炼 1–2 条可操作方法，再导向今日训练；禁止只用天赋策略套话敷衍。  
  ✅ 2026-08-27：`RAG_PRACTICE_ANSWER_HINT` + 练法问句检测
- [ ] **优化 Retrieve 查询词**  
  从用户句提取技能名（超脑阅读、开口窍等），拼成 `"{技能} 训练方法 练习步骤"` 再 Retrieve，减少只命中「年级/Tier 要求」切片。  
  ✅ 2026-08-27：`guide_rag_query.py` + `_gather_rag` 使用
- [ ] **策略层让步**  
  `rag_block` 非空时，弱化或跳过 `SITUATION_STRATEGY["ready_to_train"]` 里「少展开细节、导向训练」类指令（策略仍管语气，不抢 KB 事实）。  
  ✅ 2026-08-27：`resolve_strategy(kb_context=True)` + `SITUATION_STRATEGY_WITH_KB`
- [ ] **补 RAG 路由词**  
  `guide_rag_router.py` 增加：`开口窍`、`感知力`、`多元感知`、`精力恢复`、`高效作业` 等选修/视频技能。  
  ✅ 2026-08-27
- [ ] **验收**（待生产/本地 UI）
  - 「超脑阅读具体怎么练习」→ 正文含 KB 里的具体练法（非仅 Tier 数字），按钮仍可出「去今日训练」  
  - 「开口窍怎么练习」→ 有 KB 则引用，无 KB 则诚实说去训练页看示范，勿套赢者模板  
  - 日志：`guide rag mode=retrieve nodes>0` 且 `rag_sources` 非空

## P1 配置精简（降低运维复杂度，非阻塞）

目标：生产必填压到 ~5 行，开关收进代码默认值。

- [ ] **compose 去重**：`docker-compose.prod.yml` 百炼相关项只走 `env_file`，删掉与 `.env.production` 重复的 `environment` 透传
- [ ] **开关合并**：评估 `GUIDE_RAG=1` 单开关替代 `GUIDE_RAG_ENABLED` + `BAILIAN_RAG_GENERATE` + `BAILIAN_RAG_FALLBACK_DOUBAO` + `GUIDE_RAG_MODE`（稳定后 `file_search` 路径标为高级/默认关）
- [ ] **调参内收**：`BAILIAN_RETRIEVE_TOP_N`、`BAILIAN_ENABLE_RERANKING` 等只留代码默认 + 文档说明，生产 `.env` 不必写
- [ ] **health 可观测**：`/api/health` 或 `/api/guide/debug` 展示 `rag_mode: retrieve_doubao`、`retrieve_ready`、`index_id`
- [ ] 更新 `.env.production.example`：区分「生产必填」与「可选调参」两节

## P2 安全（建议）

- [ ] 聊天/文档中曾暴露的 DB、OSS、豆包、百炼、微信密钥 → 阿里云/火山控制台轮换一轮

## 不做

- 恢复 `BAILIAN_RAG_GENERATE=1` 作为默认主链路（除非单独做 A/B）
- 把压测脚本、临时 `_test_*.py` 提交进仓库
