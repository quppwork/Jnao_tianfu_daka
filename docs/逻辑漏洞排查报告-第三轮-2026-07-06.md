# 逻辑漏洞排查报告 — 第三轮（全栈）

> 日期: 2026-07-06  
> 范围: 后端 `backend/` + 前端 `vue_fronted/`  
> 排除: 并发竞态

---

## 本轮修复范围

| # | 问题 | 严重度 | 修复方式 |
|---|------|:--:|----------|
| 1 | 遗留 `/api/auth/register` 无鉴权 | 🔴 | 生产默认关闭；重复注册 409 不返 token |
| 2 | `/api/talent/report` IDOR 写 | 🔴 | 加 `get_authenticated_student` |
| 3 | `deps.py` schema 缺失降级 bypass | 🔴 | 改为 503 fail-closed |
| 4 | 前端 `ensureChildUser` 自动 guest 注册 | 🔴 | 无 session 跳转登录 |
| 5 | 家长/学生 session 混用 | 🔴 | 学生入口校验 role≠parent |
| 6 | chat/voice/resources 无鉴权 | 🔴 | 加 session + 限流（保留豆包 TTS/Chat 功能） |
| 7 | token 走 URL query | 🟡 | 改 Header + CORS |
| 8 | 学生 API 不校验 role | 🟡 | `get_authenticated_student` |
| 9 | admin 密码每次启动重置 | 🟡 | 仅首次创建设密码 |
| 10 | 任意 `plan_date` | 🟡 | 写操作限定当日训练日 |
| 11 | 选修打卡无 cutoff | 🟡 | 对齐必修 cutoff |
| 12 | 改密码不 revoke session | 🟡 | 改密后 `revoke_all_sessions` |
| 13 | admin 登录无 bruteforce 保护 | 🟡 | 复用 blacklist |
| 14 | 生产 mock SMS/captcha | 🟡 | production 启动校验 |

**不改动:** 豆包 Chat/TTS 业务逻辑与 API 对接方式，仅加鉴权与限流防护。

---

## 一、架构信任边界（排查结论）

详见各 `#` 条目；前两轮（duration_slot、unbind、选修去重等）已修复，不再重复。

---

## 二、CRITICAL 未修项（本轮处理）

### 1. 遗留注册无鉴权

**文件:** `backend/app/api/auth.py`

### 2. 天赋 report IDOR

**文件:** `backend/app/api/talent.py:59-100`  
**前端:** `vue_fronted/src/utils/userApi.js` — `submitTalentReport`

### 3. 认证降级 bypass

**文件:** `backend/app/core/deps.py:52-76`

### 4. Guest 自动注册

**文件:** `vue_fronted/src/utils/userApi.js` — `ensureChildUser`

---

## 三、HIGH 未修项（本轮处理）

### 5. 开放 chat / voice / resources

**文件:** `chat.py`, `voice.py`, `resources.py`, `talent.py` jnao 代理

### 6. Session token 在 URL

**文件:** `userApi.js`, `main.py` CORS

### 7. 角色混用

**文件:** `deps.py` + 学生路由

---

## 四、MEDIUM 未修项（本轮处理）

### 8–14

见上表「本轮修复范围」。

---

## 五、LOW（文档记录，暂不实现）

- `get_child_user_id` 弱依赖仍导出
- Dev API 仅 env 门控
- `validate_session` 每次 commit
- onboarding `resume=4` 跳过 auth
- 客户端 loginGuard 可绕过（后端已补偿）

---

## 六、修复验证

```bash
cd backend && python -m pytest -q
```

前端：学生页无登录应跳转 `/pages/login/index`；天赋提交需 session；Chat/TTS 需登录后可用。
